import React from "react";
import { View, Text } from "react-native";

const BookingFormScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Booking Form Screen</Text>
    </View>
  );
};

export default BookingFormScreen;